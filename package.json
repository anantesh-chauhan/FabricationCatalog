{
  "name": "fabrication-catalog",
  "version": "1.0.0",
  "description": "MERN stack for Fabrication Shop - Single Service Deployment",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": "18.x"
  },
  "keywords": [
    "mern",
    "fabrication",
    "catalogue"
  ],
  "author": "",
  "license": "MIT"
}
